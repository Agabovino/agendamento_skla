# =============================================================================
# BANCO DE DADOS (Supabase PostgreSQL - Externo)
# =============================================================================
# Substitua pela sua URL de conexão do Supabase
# Acesse: https://supabase.com → Project → Settings → Database → Connection String
# Use o formato "URI" (transaction mode recomendado para ambientes serverless):
DATABASE_URL=postgresql://postgres:SUA-SENHA-URL-ENCODED@aws-1-us-east-1.pooler.supabase.com:6543/postgres?pgbouncer=true

# =============================================================================
# GOOGLE OAUTH (Google Cloud Console)
# =============================================================================
GOOGLE_CLIENT_ID=SEU_GOOGLE_CLIENT_ID
GOOGLE_CLIENT_SECRET=SEU_GOOGLE_CLIENT_SECRET
# IMPORTANTE: Atualize com seu domínio real (temporário ou final da Hostinger)
GOOGLE_REDIRECT_URI=https://SEU_DOMINIO/api/auth/google/callback

# =============================================================================
# SEGURANÇA (Troque antes de colocar em produção!)
# =============================================================================
ADMIN_PASSWORD=admin123
ENCRYPTION_KEY=skla_secure_key_32_chars_12345678
JWT_SECRET=skla_jwt_secret_token_87654321

# =============================================================================
# CONFIGURAÇÕES DA APLICAÇÃO
# =============================================================================
NODE_ENV=production
# Deixe em branco — a Hostinger define a PORT automaticamente via process.env.PORT
# PORT=3000
